# 简单使用指南

## 🎯 项目说明

这是一个简单的 API 服务展示页面，您可以轻松修改内容、链接和图片。

---

## 📁 项目结构

```
Claude-Use/
├── app/
│   ├── page.tsx              # 首页（API 展示页）
│   └── tutorial/page.tsx     # 使用教程页（原 Claude Code 教程）
├── config/
│   └── site.ts               # 🔧 配置文件（修改这里即可！）
├── components/               # 页面组件
└── public/
    └── uploads/              # 📸 图片存储目录
```

---

## 🚀 快速开始

### 1. 启动项目

```bash
npm run dev
```

访问 http://localhost:3000

### 2. 页面导航

- **首页** (`/`) - API 服务展示页
- **使用教程** (`/tutorial`) - Claude Code 安装教程

---

## ✏️ 如何修改内容

### 只需编辑一个文件：`config/site.ts`

所有的文字、链接、价格都在这个文件里！

#### 可以修改的内容：

1. **标题和描述** - 修改 title、subtitle、description
2. **横幅图片** - 修改 bannerImage 路径
3. **三种获取方式** - 修改 acquisitionMethods 数组
4. **套餐价格** - 修改 pricingPlans 数组
5. **联系方式** - 修改 contactInfo

---

## 🖼️ 如何添加图片

### 步骤：

1. **准备图片** - 建议尺寸 1200x600
2. **上传图片** - 复制到 `public/uploads/` 目录
3. **修改配置** - 编辑 `config/site.ts` 中的 `bannerImage: "/uploads/你的图片.jpg"`
4. **查看效果** - 刷新页面

---

## 🔗 如何修改链接

编辑 `config/site.ts` 文件：

### 修改注册链接
```typescript
actionLink: "https://your-register-link.com"
```

### 修改购买链接
```typescript
purchaseLink: "https://your-payment-link.com/basic"
```

### 修改邀请链接
```typescript
actionLink: "https://your-referral-link.com"
```

---

## 📝 修改示例

### 示例 1：修改标题

打开 `config/site.ts`，找到：

```typescript
title: "欢迎使用 Claude API 服务"
```

改成：

```typescript
title: "我的 AI API 服务平台"
```

保存后刷新页面即可看到效果！

### 示例 2：修改套餐价格

```typescript
pricingPlans: [
  {
    name: "基础版",
    price: "¥29",           // 修改价格
    tokens: "100,000 tokens",
    features: ["支持所有模型", "7x24小时服务"],
    purchaseLink: "https://your-link.com"  // 修改购买链接
  }
]
```

### 示例 3：添加新套餐

在 `pricingPlans` 数组中添加：

```typescript
{
  name: "体验版",
  price: "¥9.9",
  tokens: "50,000 tokens",
  features: ["支持 GPT-3.5", "每日限额"],
  purchaseLink: "https://your-link.com/trial"
}
```

---

## 🎯 常见操作

### 更换横幅图片

```bash
# 1. 复制图片到 uploads 目录
cp ~/Downloads/banner.jpg public/uploads/banner.jpg

# 2. 编辑 config/site.ts
bannerImage: "/uploads/banner.jpg"

# 3. 刷新页面
```

### 修改所有购买链接

统一修改为您的支付平台：

```typescript
purchaseLink: "https://pay.yoursite.com?plan=basic"
purchaseLink: "https://pay.yoursite.com?plan=pro"
purchaseLink: "https://pay.yoursite.com?plan=enterprise"
```

---

## ⚠️ 注意事项

1. **保存文件** - 修改后记得保存 `config/site.ts`
2. **图片路径** - 图片放在 `public/uploads/`，配置中写 `/uploads/图片名.jpg`
3. **链接格式** - 外部链接用完整 URL：`https://example.com`
4. **语法正确** - 注意逗号、引号等符号

---

## 🎉 总结

### 您只需要：

✅ 编辑 `config/site.ts` 修改文字和链接  
✅ 上传图片到 `public/uploads/` 目录  
✅ 刷新页面查看效果

### 不需要：

❌ 不需要数据库  
❌ 不需要后端 API  
❌ 不需要登录系统  
❌ 不需要复杂配置

**就是这么简单！** 🚀

---

## 📞 需要帮助？

如果遇到问题，检查：
1. 文件路径是否正确
2. 语法是否有错误（逗号、引号等）
3. 开发服务器是否正在运行

祝使用愉快！✨
